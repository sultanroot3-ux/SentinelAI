fastapi
uvicorn[standard]
sqlalchemy
alembic
pydantic
pydantic-settings
email-validator
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
python-multipart
# OpenCV 5.x removed the legacy CascadeClassifier API the Haar fallback uses
opencv-python>=4.13.0.92,<5
numpy
# PostgreSQL driver (SQLite needs nothing extra)
psycopg2-binary
# Notifications + report exports
requests
reportlab
openpyxl

# --- Optional: real face recognition (512-d embeddings, buffalo_l model) ---
# Install these to enable actual face matching; without them the backend falls
# back to OpenCV Haar-cascade detection only (no recognition).
# insightface
# onnxruntime
